const configDatabase = {
  host: 'localhost',
  dialect: 'mysql',
  user: 'baq',
  password: 'Asiste.2021',
  database: 'database_app'
}

const port = 8000;

module.exports = {
  configDatabase,
  port
}